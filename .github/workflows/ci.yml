name: tests

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.11"]
<<<<<<< HEAD
=======
        # Both NumPy majors, on purpose. The shipped pin is `numpy<2` because
        # torch 2.0.1 -- the build recommended for a CUDA 11.4 driver --
        # predates NumPy 2 C-API support. That is a *torch* constraint, not
        # ours, so the code must keep working on either: NumPy 2 added
        # `np.bitwise_count`, which polyptail/data/phash.py uses when present
        # and emulates with a lookup table when absent, and only this matrix
        # exercises both branches.
        numpy: ["<2", ">=2"]
    name: py${{ matrix.python-version }} numpy${{ matrix.numpy }}
>>>>>>> 76f695f8eb8f88345c44aead928ed10bcb542ec6
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install (CPU torch)
        run: |
          python -m pip install --upgrade pip
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install -r requirements-dev.txt
<<<<<<< HEAD
=======
          # Override the shipped ceiling for the numpy>=2 leg of the matrix.
          pip install "numpy${{ matrix.numpy }}"
          python -c "import numpy, torch; print('numpy', numpy.__version__, 'torch', torch.__version__)"
>>>>>>> 76f695f8eb8f88345c44aead928ed10bcb542ec6
      - name: Unit tests
        run: pytest -q
      - name: End-to-end smoke run on synthetic data
        run: |
          python tools/make_smoke_data.py --out ./_smoke_data
<<<<<<< HEAD
=======
          # The layout checker must reject a tree that is not the PraNet
          # distribution; the smoke data deliberately is not one.
          python tools/prepare_data.py --root ./_smoke_data --check && exit 1 || true
>>>>>>> 76f695f8eb8f88345c44aead928ed10bcb542ec6
          python tools/verify_manifest.py --root ./_smoke_data --manifest ./_smoke_data/manifest.json
          python tools/hash_collisions.py --root ./_smoke_data \
            --manifest ./_smoke_data/manifest.json --out ./_smoke_data/collisions.json --quiet
          python tools/train.py --config configs/smoke.yaml run.name=ci
          python tools/evaluate.py --run _smoke_runs/ci --compare
<<<<<<< HEAD
=======

  conda:
    runs-on: ubuntu-latest
    name: conda env resolves
    steps:
      - uses: actions/checkout@v4
      - uses: conda-incubator/setup-miniconda@v3
        with:
          environment-file: environment-cpu.yml
          activate-environment: polyptail-cpu
          auto-activate-base: false
      - name: Unit tests in the conda environment
        shell: bash -el {0}
        run: |
          python -c "import numpy, torch; print('numpy', numpy.__version__, 'torch', torch.__version__)"
          pytest -q
>>>>>>> 76f695f8eb8f88345c44aead928ed10bcb542ec6
